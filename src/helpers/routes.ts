interface IRouter {
  home: string;
  resume: string;
  projects: string;
  admin: string;
}

export const routers: IRouter = {
  home: '/',
  resume: 'resume',
  projects: 'projects',
  admin: 'admin',
};
